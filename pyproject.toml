[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "webscope"
version = "1.0.0"
description = "Website transport, TLS, DNS and security-header inspector"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "meduuv"}]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent"
]

[project.scripts]
webscope = "webscope.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
